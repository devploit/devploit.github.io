{
  "name": "devploit-site",
  "private": true,
  "scripts": {
    "build": "node scripts/render-cves.mjs",
    "check": "node scripts/render-cves.mjs --check && node scripts/check-site.mjs",
    "check:browser": "node scripts/check-browser.mjs"
  }
}
